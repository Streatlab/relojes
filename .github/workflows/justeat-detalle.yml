name: Just Eat · detalle de pedidos (historial)

# Robot de detalle de pedidos propios de Just Eat (Partner Hub). Lee el Historial,
# abre cada pedido nuevo y guarda líneas/totales/hora/marca en justeat_pedidos +
# justeat_pedido_lineas. NO toca la facturación oficial (liquidación quincenal).
# LEY DE RELOJES + LEY BUEN CIUDADANO (freno de caudal + jitter dentro del script).
#
# ══════════════════════════════════════════════════════════════════════════
# ⚠️ 31-ago · ESTE FICHERO SE HA PISADO DOS VECES. LEER ANTES DE TOCARLO.
#
# (1) De madrugada se reescribió el YAML entero cinco veces seguidas para
#     arreglar el arranque de Camoufox, y en cada pasada se perdieron sin querer
#     las tres cosas que SÍ hacían falta: el paso de WARP, `headless=False` con
#     pantalla virtual, y las cookies sin particionar. Las corridas #198 a #201
#     no llevaban nada de eso, y las conclusiones que salieron de ellas eran ruido.
#
# (2) A las 13:20, reescribiendo otra vez para añadir alternativas de arranque,
#     se perdió UNA SOLA LÍNEA: la que publica `CAMOUFOX_WS` para el paso
#     siguiente. El servidor levantó bien, pero el robot recibió la variable
#     vacía, se fue a Chromium y murió pidiendo instalar navegadores. Un fallo
#     que parecía de Playwright y era de una línea borrada.
#
# LECCIÓN, ya por partida doble: no reescribir el fichero entero para tocar tres
# líneas; y si se reescribe, comparar con lo que había antes de dar por bueno
# nada de lo que salga después.
# ══════════════════════════════════════════════════════════════════════════
#
# ── 31-ago · ARRANQUE DEL NAVEGADOR CON ALTERNATIVAS (palanca 25) ─────────
# `launch_server(...)` empezó a morir dentro de `launch_options(**kwargs)` sin
# que el montaje hubiera cambiado. La explicación: Camoufox SE INSTALA FRESCO EN
# CADA CORRIDA, así que una versión suya puede dejar de aceptar un argumento de
# un día para otro. Ahora se prueban tres montajes, del más completo al más
# pelado, y se usa el primero que levante (queda apuntado cuál entró):
#     1. cookies abiertas + permiso explícito   2. cookies abiertas   3. pelado
# REGLA: toda dependencia que se instala fresca en cada corrida puede cambiarte
# la firma mañana. Si el arranque depende de ella, hay que tener plan B.
#
# ── QUÉ SE HA PROBADO EN LA PUERTA DE `access.just-eat.es` (30-ago) ───────
# El Partner Hub pasa sin problema con Camoufox. La que no se cruza es la puerta
# de acceso (Keycloak). Descartado, uno a uno y con medición:
#   · sesión mezclada Chromium→Firefox ....... arreglado, seguía igual
#   · chivato con falso positivo ............. arreglado, seguía igual
#   · casilla del Turnstile sin pulsar ....... NO HAY casilla: `iframes=0`
#   · idioma/huso impuestos al navegador ..... quitados, seguía igual
#   · desafío atascado, recargar ............. dos recargas, seguía igual
#   · cookies particionadas .................. abiertas, seguía igual
# Lo que SÍ la cruza: salir por WARP (palanca 23).
#
# ── CÓMO SE LANZA A MANO ──────────────────────────────────────────────────
# Botón "Run workflow" en Actions, o editando `disparos/justeat-detalle.txt`.
# Las dos vías hacen PUESTA AL DÍA: leen el historial a cualquier hora. Por reloj
# manda la ventana de servicio.
#
# palanca: 6 (19-ago) — cruza la puerta de re-login al abrir el historial
# palanca: 7 (22-ago) — 2 cruces de re-login, detección robusta del historial
# palanca: 8 (22-ago) — rescate de días caídos
# palanca: 16 (29-ago) — pin de Playwright a la versión de Camoufox
# palanca: 21 (30-ago) — sin idioma forzado, pantalla virtual, disparo aparte
# palanca: 22 (30-ago) — cookies sin particionar
# palanca: 23 (30-ago) — salida por WARP
# palanca: 25 (31-ago) — arranque con alternativas
# palanca: 26 (31-ago) — restaurada la línea que publica CAMOUFOX_WS, y el paso
#   del robot ya NO arranca si esa variable viene vacía (antes se caía a Chromium
#   en silencio, que es justo lo que no se quiere)
on:
  schedule:
    - cron: "5,35 10-15 * * *"
    - cron: "5,35 17-22 * * *"
  workflow_dispatch:
    inputs:
      puesta_al_dia:
        description: "Leer el historial aunque sea fuera de horas de servicio (backfill)"
        type: boolean
        default: true
  push:
    paths:
      - ".github/workflows/justeat-detalle.yml"
      - "disparos/justeat-detalle.txt"

jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      seguir: ${{ steps.candado.outputs.seguir }}
    steps:
      - name: Candado de franja de servicio (Madrid)
        id: candado
        run: |
          H=$(TZ=Europe/Madrid date +%-H)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || [ "${{ github.event_name }}" = "push" ]; then
            echo "seguir=1" >> $GITHUB_OUTPUT; exit 0
          fi
          if { [ "$H" -ge 12 ] && [ "$H" -le 16 ]; } || { [ "$H" -ge 19 ] && [ "$H" -le 23 ]; }; then
            echo "seguir=1" >> $GITHUB_OUTPUT
          else
            echo "seguir=0" >> $GITHUB_OUTPUT
          fi

  robot:
    needs: plan
    if: needs.plan.outputs.seguir == '1'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: justeat-detalle
      cancel-in-progress: true
    env:
      SB_URL: https://eryauogxcpbgdryeimdq.supabase.co
      SB_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      # LA TELEMETRÍA SE ESCRIBE CON HEREDOC ENTRECOMILLADO: dentro de <<'SH' el
      # shell no interpreta nada, así que las comillas van tal cual.
      - name: Fase 1 · arranque
        run: |
          cat > /tmp/apunta.sh <<'SH'
          apunta() {
            jq -n --arg e "$1" --arg d "$2" \
              '{fuente:"justeat_job",estado:$e,detalle:$d}' \
            | curl -s -X POST "$SB_URL/rest/v1/robot_log" \
                -H "apikey: $SB_KEY" \
                -H "Authorization: Bearer $SB_KEY" \
                -H "Content-Type: application/json" \
                --data-binary @- > /dev/null || true
          }
          SH
          chmod +x /tmp/apunta.sh
          source /tmp/apunta.sh
          apunta "fase_1_arranque" "job iniciado ($(TZ=Europe/Madrid date '+%H:%M') Madrid, evento ${{ github.event_name }}) · palanca 26"
          echo "telemetria lista"

      # SALIDA POR LA RED DE CLOUDFLARE. Nunca tumba el job: si no levanta, se
      # apunta y la corrida sigue saliendo como antes.
      - name: Salir por WARP
        continue-on-error: true
        shell: bash +e {0}
        run: |
          source /tmp/apunta.sh
          ANTES=$(curl -s --max-time 15 https://www.cloudflare.com/cdn-cgi/trace | tr '\n' ' ')
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg \
            | sudo gpg --yes --dearmor -o /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" \
            | sudo tee /etc/apt/sources.list.d/cloudflare-client.list > /dev/null
          sudo apt-get update -qq && sudo apt-get install -y -qq cloudflare-warp > warp.log 2>&1
          if [ $? -ne 0 ]; then
            apunta "warp_no_instala" "$(tail -c 500 warp.log)"
            exit 0
          fi
          sudo warp-cli --accept-tos registration new >> warp.log 2>&1
          sudo warp-cli --accept-tos connect >> warp.log 2>&1
          for i in $(seq 1 20); do
            TRACE=$(curl -s --max-time 10 https://www.cloudflare.com/cdn-cgi/trace | tr '\n' ' ')
            case "$TRACE" in
              *"warp=on"*)
                apunta "warp_ok" "saliendo por WARP · antes:[$(echo "$ANTES" | cut -c1-140)] ahora:[$(echo "$TRACE" | cut -c1-140)]"
                exit 0;;
            esac
            sleep 3
          done
          apunta "warp_ko" "WARP no se ha conectado, sigo con la salida de siempre · $(tail -c 400 warp.log)"
          exit 0

      - name: Jitter humano
        if: github.event_name == 'schedule'
        run: sleep $(( RANDOM % 181 ))

      - uses: actions/checkout@v4
        with:
          repository: Streatlab/binagre
          ref: trabajo
          token: ${{ secrets.GH_PAT }}
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Instalar dependencias
        run: |
          source /tmp/apunta.sh
          npm install --no-audit --no-fund > npm.log 2>&1 || { apunta "fase_2_npm_ko" "$(tail -c 800 npm.log)"; exit 1; }
          apunta "fase_2_npm_ok" "dependencias instaladas"

      - name: Instalar Camoufox
        run: |
          source /tmp/apunta.sh
          sudo apt-get update -qq && sudo apt-get install -y -qq xvfb
          (pip install --quiet "camoufox[geoip]" || pip install --quiet camoufox) > cam.log 2>&1 \
            || { apunta "fase_3_pip_ko" "$(tail -c 800 cam.log)"; exit 1; }
          python -m camoufox fetch >> cam.log 2>&1 \
            || { apunta "fase_3_fetch_ko" "$(tail -c 800 cam.log)"; exit 1; }
          VERCAM=$(python3 -c "import importlib.metadata as m; print(m.version('camoufox'))" 2>/dev/null || echo "?")
          apunta "fase_3_camoufox_ok" "camoufox instalado y descargado · version $VERCAM"

      # El cliente de Playwright se alinea con el del servidor de Camoufox; si no
      # coinciden, la conexión muere con "428 Precondition Required". `--no-save`:
      # el package.json no se toca, esto vive solo dentro de esta corrida.
      - name: Alinear Playwright con el de Camoufox
        run: |
          source /tmp/apunta.sh
          VER=$(python3 -c "import importlib.metadata as m; print(m.version('playwright'))" 2>/dev/null || echo "")
          if [ -z "$VER" ]; then
            apunta "fase_3b_sin_version" "no he podido leer la version de playwright de camoufox; sigo con la del repo"
          else
            npm install --no-save --no-audit --no-fund "playwright@$VER" "playwright-core@$VER" > pw.log 2>&1 \
              || { apunta "fase_3b_pin_ko" "no he podido instalar playwright@$VER · $(tail -c 600 pw.log)"; exit 1; }
            apunta "fase_3b_pin_ok" "cliente Playwright alineado a la version del servidor de Camoufox: $VER"
          fi

      - name: Levantar el servidor de Camoufox
        # `bash +e`: aquí los fallos son ESPERADOS mientras el servidor levanta.
        shell: bash +e {0}
        run: |
          source /tmp/apunta.sh
          cat > servidor_camoufox.py <<'PY'
          import traceback
          from camoufox.server import launch_server

          # Cookies sin particionar (palanca 22): con la protección antirrastreo
          # de serie, el desafío de Cloudflare no puede guardar su propia galleta.
          PREFS = {
              "network.cookie.cookieBehavior": 0,
              "privacy.partition.network_state": False,
              "privacy.trackingprotection.enabled": False,
              "privacy.trackingprotection.pbmode.enabled": False,
          }

          # headless=False + xvfb: para Cloudflare es una pantalla real. NO
          # cambiar a True. Sin locale forzado: Camoufox cuadra idioma y huso con
          # la IP real por la que se sale.
          BASE = dict(headless=False, os=["windows"], humanize=True,
                      geoip=True, port=4545, ws_path="binagre")

          INTENTOS = [
              ("cookies abiertas + permiso explicito",
               dict(firefox_user_prefs=PREFS, i_know_what_im_doing=True)),
              ("cookies abiertas", dict(firefox_user_prefs=PREFS)),
              ("sin tocar cookies", dict()),
          ]

          for nombre, extra in INTENTOS:
              try:
                  print(f"ARRANQUE probando: {nombre}", flush=True)
                  launch_server(**BASE, **extra)   # bloquea si arranca bien
                  break
              except Exception as e:
                  print(f"ARRANQUE fallo con '{nombre}': {type(e).__name__}: {e}", flush=True)
                  traceback.print_exc()
          PY
          nohup xvfb-run -a python servidor_camoufox.py > camoufox_servidor.log 2>&1 &
          echo $! > camoufox.pid

          cat > espera_puerto.py <<'PY'
          import socket, sys
          for fam, host in ((socket.AF_INET6, "::1"), (socket.AF_INET, "127.0.0.1")):
              s = socket.socket(fam, socket.SOCK_STREAM)
              s.settimeout(1)
              try:
                  s.connect((host, 4545))
                  print("[" + host + "]" if fam == socket.AF_INET6 else host)
                  sys.exit(0)
              except OSError:
                  pass
              finally:
                  s.close()
          sys.exit(1)
          PY

          HOST=""
          for i in $(seq 1 120); do
            if SALIDA=$(python3 espera_puerto.py); then
              HOST="$SALIDA"
              break
            fi
            if ! kill -0 "$(cat camoufox.pid)" 2>/dev/null; then
              apunta "fase_4_servidor_muerto" "$(grep ARRANQUE camoufox_servidor.log | tail -c 500) || $(tail -c 700 camoufox_servidor.log)"
              cat camoufox_servidor.log
              exit 1
            fi
            sleep 1
          done

          if [ -z "$HOST" ]; then
            apunta "fase_4_servidor_sin_puerto" "no escucha en 4545 tras 120s · $(tail -c 700 camoufox_servidor.log)"
            cat camoufox_servidor.log
            exit 1
          fi

          # ESTA LÍNEA ES LA QUE SE PERDIÓ EL 31-ago A LAS 13:20. Sin ella el
          # robot recibe la variable vacía, se va a Chromium y muere pidiendo
          # instalar navegadores. No borrarla.
          echo "CAMOUFOX_WS=ws://${HOST}:4545/binagre" >> $GITHUB_ENV

          MODO=$(grep "ARRANQUE probando" camoufox_servidor.log | tail -1)
          apunta "fase_4_camoufox_arriba" "escuchando en ${HOST}:4545 · $MODO"

      - name: Leer historial de pedidos de Just Eat
        env:
          SUPABASE_URL: https://eryauogxcpbgdryeimdq.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          JE_FORZAR: ${{ (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.puesta_al_dia)) && '1' || '' }}
        run: |
          source /tmp/apunta.sh
          # Sin Camoufox NO se corre: caer a Chromium en silencio es lo que tiró
          # 110 corridas contra el muro en agosto.
          if [ -z "$CAMOUFOX_WS" ]; then
            apunta "fase_5_sin_camoufox" "CAMOUFOX_WS vacia: no arranco el robot para no salir con Chromium"
            exit 1
          fi
          apunta "fase_5_robot" "lanzando justeat-detalle.ts contra $CAMOUFOX_WS (puesta al dia=${JE_FORZAR:-0})"
          npx tsx scripts/robot-ingesta/justeat-detalle.ts 2>&1 | tee robot.log
          CODIGO=${PIPESTATUS[0]}
          if [ "$CODIGO" != "0" ]; then apunta "fase_5_robot_ko" "salida $CODIGO · $(tail -c 900 robot.log)"; exit "$CODIGO"; fi
          apunta "fase_5_robot_ok" "el robot ha terminado sin error"

      - name: Cierre
        if: always()
        run: |
          source /tmp/apunta.sh || true
          apunta "fin" "job terminado con estado ${{ job.status }}" || true
          tail -60 camoufox_servidor.log || true
